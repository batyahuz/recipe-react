import { Typography } from "@mui/material";
import ErrorIcon from '@mui/icons-material/Error';
import { RedColor } from "./colors";

const ErrorUserId = () => {
    return <div>
        שגיאה שהמשתמש לא מחובר
    </div>
}

export default ErrorUserId;